declare global {
  namespace JSX {
    interface IntrinsicElements {
      div: { className?: string; style?: Record<string, string>; children?: unknown };
    }
    interface Element { readonly _b: unique symbol }
  }
}
const accent: number = 4;
export const Card = (
  <div css=@(
    display: flex;
    border-left: {{accent.toUpperCase()}};
  )>
    hi
  </div>
);
