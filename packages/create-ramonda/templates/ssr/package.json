{
  "name": "ramonda-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build:bundles": "node scripts/build.mjs",
    "build": "ramonda-check && npm run build:bundles && node scripts/prerender.mjs && ramonda-check-bundle dist",
    "start": "node server.mjs --prod"
  }
}
