{
  "name": "ramonda-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "ramonda-check && vite build && ramonda-check-bundle dist",
    "preview": "vite preview"
  }
}
