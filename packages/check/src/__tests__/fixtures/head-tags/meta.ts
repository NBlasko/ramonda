/** Page metadata kept in a module of its own, which is where it ends up on a real site. */
export const PAGE_HEAD = {
  description: "the shorthand",
  meta: [{ name: "description", content: "the list" }],
};

export const ROBOTS = "robots";
