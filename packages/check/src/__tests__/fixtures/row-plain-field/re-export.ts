/**
 * The framework's own `list`, handed on under this module's name.
 *
 * An app that wraps its imports in a `ui` module is doing something ordinary, and the framework's
 * `list` is still the framework's `list` — the rows it builds are cached the same way.
 */
export { list } from "@ramonda/core";
