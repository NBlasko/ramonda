{
  "name": "ramonda-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check-types": "turbo run check-types",
    "check": "node scripts/preflight-node.mjs && pnpm install --frozen-lockfile && node scripts/check-workflows.mjs && node scripts/check-changesets.mjs && pnpm --filter @ramonda/check build && node packages/check/dist/cli.js apps/docs/tsconfig.json && node packages/check/dist/cli.js apps/docs/tsconfig.json --fix --dry-run && pnpm lint && pnpm format:check && turbo run check-types test build && node scripts/check-examples.mjs && node scripts/build-package-readmes.mjs --check && node scripts/check-side-effects.mjs && node scripts/check-bare-import.mjs && node scripts/check-tsconfigs.mjs && node scripts/check-decorator-duplication.mjs && pnpm check:scaffold",
    "check:workflows": "node scripts/check-workflows.mjs",
    "check:changesets": "node scripts/check-changesets.mjs",
    "check:examples": "node scripts/check-examples.mjs",
    "check:readmes": "node scripts/build-package-readmes.mjs --check",
    "check:side-effects": "node scripts/check-side-effects.mjs",
    "check:bare-import": "node scripts/check-bare-import.mjs",
    "changeset": "changeset",
    "release": "pnpm build && pnpm --filter create-ramonda verify-versions && changeset publish",
    "check:scaffold": "node scripts/check-scaffold.mjs ssr && node scripts/check-scaffold.mjs spa",
    "check:tsconfigs": "node scripts/check-tsconfigs.mjs",
    "check:decorators": "node scripts/check-decorator-duplication.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@changesets/changelog-github": "^1.0.0",
    "@changesets/cli": "^3.0.1",
    "@vitest/coverage-v8": "^4.1.11",
    "oxlint": "^1.81.0",
    "turbo": "^2.10.12",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1"
    }
  },
  "dependencies": {
    "@ramonda/check": "workspace:^"
  }
}
