{
  "name": "ramonda-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check-types": "turbo run check-types",
    "check": "node scripts/preflight-node.mjs && node scripts/check-workflows.mjs && pnpm --filter @ramonda/check build && node packages/check/dist/cli.js apps/docs/tsconfig.json && pnpm lint && pnpm format:check && turbo run check-types test build && node scripts/check-examples.mjs && node scripts/check-side-effects.mjs",
    "check:workflows": "node scripts/check-workflows.mjs",
    "check:examples": "node scripts/check-examples.mjs",
    "check:side-effects": "node scripts/check-side-effects.mjs",
    "changeset": "changeset",
    "release": "pnpm build && pnpm --filter create-ramonda verify-versions && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.1",
    "@vitest/coverage-v8": "^4.1.10",
    "oxlint": "^1.77.0",
    "turbo": "^2.10.8",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1"
    }
  },
  "dependencies": {
    "@ramonda/shared": "workspace:^"
  }
}
